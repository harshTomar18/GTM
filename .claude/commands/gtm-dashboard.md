---
description: Render a one-shot text dashboard for a tenant.
argument-hint: tenant=<id>
---

Arguments: $ARGUMENTS

Required: `tenant=<id>`.

Invoke the **gtm-dashboard** skill. Render the result.
